// TODO: requerir los controladores de cada ruta y asignarlos a las rutas (exportar el router)
const { orderRouter } = require('./controller/orders/orders.controller.js');

module.exports = {
    orderRouter
}